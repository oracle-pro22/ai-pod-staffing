# Phase 2 acceptance checklist

- [x] Existing HTML prototype remains the live root experience.
- [x] React preview has an isolated route.
- [x] Workbook data crosses a typed server boundary.
- [x] Request data has a canonical normalized model.
- [x] Role rules are defined once and consumed by navigation and selectors.
- [x] Role-scoped identities and data are derived rather than copied into components.
- [x] Shared UI controls are reusable React components.
- [x] Original icon shapes are implemented as React SVG components.
- [x] Shared shell matches the approved colors, dimensions, spacing, and responsive rules.
- [x] Notifications and Ask AI Pod are functional in the React shell.
- [x] Production build and TypeScript validation pass.
- [x] No Excel write-back was introduced.
